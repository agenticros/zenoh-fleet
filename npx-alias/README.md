# zenoh-fleet

Unscoped `npx` entry for [`@agenticros/zenoh-fleet`](https://www.npmjs.com/package/@agenticros/zenoh-fleet):

```bash
npx zenoh-fleet
```

Same wizard and binaries as the scoped package. Docs: https://github.com/agenticros/zenoh-fleet
